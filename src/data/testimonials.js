/* Keep this empty until a client provides a real quote and permission to publish it.
 * Shape: { quote, name, role, company, avatar, profileUrl, projectSlug }
 */
const testimonials = [];

export default testimonials;
